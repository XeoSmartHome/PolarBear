import React from 'react';
import { StyleSheet } from 'react-native';

type RecipeImporterScreenProps = {}

const RecipeImporterScreen = ({}: RecipeImporterScreenProps) => {
    return null;
};

const styles = StyleSheet.create({});

export default RecipeImporterScreen;
